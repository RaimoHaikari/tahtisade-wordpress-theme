import React from 'react';

const MovieCard = () => {
    return (
        <div style={{height: "450px", background: "green"}}>
            Elokuvan tiedot
        </div>
    );
};

export default MovieCard;